package pe.upao.alquiler.modelo;

/** Ciclo de vida de una máquina (HU-08): BORRADOR -> PUBLICADA -> RETIRADA (y de vuelta a PUBLICADA). */
public enum EstadoMaquina {
    BORRADOR,
    PUBLICADA,
    RETIRADA
}
